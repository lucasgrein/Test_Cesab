# I. Load functions and Download data 

# library(ggplot2)

# devtools::install_deps(upgrade = "never")
# devtools::load_all()

# source( here::here("03_analyses", "download-data.R") )

# quarto::quarto_render(input=here::here("03_analyses", "index.qmd") )

targets::tar_make()
